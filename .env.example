# console | resend | smtp
MAIL_TRANSPORT=resend

# Resend (https://resend.com)
RESEND_API_KEY=
RESEND_FROM="Airbnb <onboarding@resend.dev>"

# SMTP (optionnel, si MAIL_TRANSPORT=smtp)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
