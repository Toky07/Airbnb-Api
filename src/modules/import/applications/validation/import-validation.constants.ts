export const TIME_PATTERN = /^([01]\d|2[0-3]):[0-5]\d$/;

export const ROOM_STATUSES = new Set(['available', 'occupied', 'maintenance']);

export const ROLE_SLUG_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
